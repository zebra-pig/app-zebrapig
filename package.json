{
  "name": "@zebrapig/monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "pnpm --filter web-client build",
    "dev": "pnpm --filter web-client dev",
    "build:web": "pnpm --filter web-client build"
  }
}
