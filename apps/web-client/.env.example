
# erp-api GraphQL endpoint for the public /t/<token> page (resolveGearTag).
API_GQL_ENDPOINT=https://api.zebrapig.com/graphql
CONTENT_ENDPOINT=
# Only needed if the web-client calls bearer-guarded API queries server-side.
API_BEARER_TOKEN=
